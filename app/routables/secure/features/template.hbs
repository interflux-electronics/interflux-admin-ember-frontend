<Page::LeftNav @id="features">
  <section>
    <h1>Features</h1>

    <Spreadsheet
      @records={{@model}}
      @columns={{array
        (hash key='name' label='Name')
        (hash key='category' label="category")
      }}
      @linkRoute="secure.features.feature"
    />
  </section>
</Page::LeftNav>