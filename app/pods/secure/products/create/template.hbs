{{#let @model.product as |product|}}
  <Modal @id='family-create'>
    <h1>Create new product</h1>

    <Field::String
      @label='Product name'
      @record={{product}}
      @attribute='name'
      @beforeSave={{this.beforeSave}}
      @afterSave={{this.afterSave}}
    />
  </Modal>
{{/let}}