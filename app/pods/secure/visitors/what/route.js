import BaseRoute from 'interflux/pods/base/route';
// import { hash } from 'rsvp';

export default class VideoRoute extends BaseRoute {
  // model(params) {
  //   return hash({
  //     video: this.store.findRecord('video', params.id, {
  //       include: ['product_videos', 'products'].join(',')
  //     })
  //   });
  // }
}
