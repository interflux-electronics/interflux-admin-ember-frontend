<Page::Content @id="images">
  <h1>Images</h1>

  <Spreadsheet
    @records={{@model.images}}
    @columns={{array
        (hash key='path' label='Path')
        (hash key='formats' label='Formats')
      }}
    @linkRoute='secure.images.image'
  />
</Page::Content>