<Page::Content @id='images' @theme='white'>
  <ListView
    @title='Images'
    @records={{this.records}}
    @config={{this.config}}
    @filters={{this.filters}}
    @buttons={{this.buttons}}
    @layouts={{array 'grid' 'list'}}
    @onClickRecord={{this.onClickRecord}}
    @onClickButton={{this.onClickButton}}
  />
</Page::Content>

{{outlet}}