<Page::Content @id='permalinks' @theme='white'>
  <ListView
    @title='Permalinks'
    @records={{this.records}}
    @config={{this.config}}
    @filters={{this.filters}}
    @buttons={{this.buttons}}
    @layouts={{array 'table'}}
    @sortBy='host'
    @onClickRecord={{this.onClickRecord}}
    @onClickButton={{this.onClickButton}}
  />
</Page::Content>

{{outlet}}