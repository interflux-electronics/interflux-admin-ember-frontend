<Page::Content @id='translations' @theme='white'>
  <ListView
    @title='Translations'
    @records={{this.records}}
    @config={{this.config}}
    @filters={{this.filters}}
    @buttons={{this.buttons}}
    @layouts={{array 'list' 'table'}}
    @onClickRecord={{this.onClickRecord}}
    @onClickButton={{this.onClickButton}}
  />
</Page::Content>

{{outlet}}