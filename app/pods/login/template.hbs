<Page @id="login">
  <Section>
    <form>
      <div class="row one">
        <Input
          @id="email"
          @value={{this.email}}
          @placeholder="Email"
          @type="email"
          @theme="tertiary"
          @disabled={{this.auth.getToken.isRunning}}
          @onChange={{fn (mut this.email)}}
          @onEnter={{this.submit}}
          @onKeyDown={{this.onKeyDown}}
          @onKeyUp={{this.onKeyUp}}
        />
      </div>

      <div class="row two">
        <Input
          @id="password"
          @value={{this.password}}
          @placeholder="Password"
          @type="password"
          @theme="tertiary"
          @disabled={{this.auth.getToken.isRunning}}
          @onChange={{fn (mut this.password)}}
          @onEnter={{this.submit}}
          @onKeyDown={{this.onKeyDown}}
          @onKeyUp={{this.onKeyUp}}
        />

        <Button
          @theme="tertiary"
          @icon={{if this.isBusy 'ellipsis' 'chevron-right'}}
          @onClick={{this.submit}}
          @isBusy={{this.isBusy}}
        />
      </div>

      {{#if this.auth.error}}
        <div class="row three">
          <p>Error: {{this.auth.error}}</p>
        </div>
      {{/if}}
    </form>

    <div class="back">
      <Button
        @route="index"
        @theme="tertiary"
        @icon="chevron-left"
      />
    </div>
  </Section>
</Page>