<fieldset
  id="field-{{@id}}"
  class={{this.classes}}
>
  <div class="liner">
    {{#if @label}}
      <label for="input-{{@id}}">{{@label}}</label>
    {{/if}}
    {{#if @legend}}
      <legend>{{@legend}}</legend>
    {{/if}}
    {{yield}}
  </div>
</fieldset>