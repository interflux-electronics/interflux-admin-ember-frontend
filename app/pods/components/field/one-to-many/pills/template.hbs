<Fieldset @label={{@label}} @legend={{@legend}}>
  <Pills::SelectOne
    @options={{@options}}
    @optionLabel={{@optionLabel}}
    @layout={{@layout}}
    @onSelect={{this.onSelect}}
    @selected={{@selected}}
  />
</Fieldset>