<Fieldset
  @id={{this.id}}
  @label={{@label}}
  @component="boolean"
  @theme={{@theme}}
  @state={{this.state}}
>
  <Checkbox
    @id={{this.id}}
    @checked={{this.value}}
    @onClick={{this.onClick}}
  />
</Fieldset>