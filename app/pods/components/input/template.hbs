<div
  class="input {{if @size @size 'no-size'}} {{if @icon 'has-icon' 'no-icon'}} {{if @disabled 'disabled' 'not-disabled'}} {{if @isValidated (if @isValid 'valid' 'invalid') 'not-validated'}}"
>
  <input
    type={{@type}}
    placeholder={{@placeholder}}
    disabled={{@disabled}}
    {{on 'focus' onFocus}}
    {{on 'blur' onBlur}}
    {{on 'keyup' onKeyUp}}
  >
  {{#if @icon}}
    <div class="icon">
      <!-- <Icon @name={{@icon}} /> -->
    </div>
  {{/if}}
</div>