<div class="filter-list">
  <header>
    <Input
      @type="search"
      @onKeyUp={{this.onKeyUp}}
      @size="medium"
    />
  </header>

  <p><strong>{{this.count}}</strong> results out of {{max}}</p>

  <ul>
    {{#if @options}}
      {{#each options as |option index|}}
        <li>
          <Button::Route
            @route={{@linkRoute}}
            @model={{get option @linkModel}}
            @text={{get option @label}}
            @size="small"
            @style="blue-3"
          />
        </li>
      {{/each}}
    {{/if}}
  </ul>
</div>